<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <runtime>
    <!-- Allow the .NET Framework to load assemblies that Windows marked as "downloaded from the internet"
         (Zone.Identifier / Mark of the Web) - e.g. after extracting the portable zip with Explorer. -->
    <loadFromRemoteSources enabled="true"/>
  </runtime>
</configuration>
